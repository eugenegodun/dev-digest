import type { SkillType } from "@devdigest/shared";

/** Selectable skill types for the Config tab SelectInput. */
export const SKILL_TYPE_OPTIONS: readonly SkillType[] = ["rubric", "convention", "security", "custom"];
